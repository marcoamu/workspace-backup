# 📅 Sesión - 2026-04-02

## Resumen del Día

### mibimax-app - Finanzas

#### Bugs Arreglados
1. **Import CSV** - Endpoint `/api/finanzas/import` no existía → creado
2. **Formato fecha** - CSV usa DD/MM/YYYY, PostgreSQL espera YYYY-MM-DD → función parse_fecha()
3. **Parser números** - Formato europeo 1.234,56 → parseEuropeanNumber()
4. **Signo negativo** - Gastos deben guardarse positivos, tipo="egreso"
5. **Categoría por defecto** - Cambiado de "importado" a "Otros"
6. **Filtro año** - Ahora envía año al API (antes no funcionaba)
7. **renderMesCharts** - Función que no existía → añadida
8. **Proxy Vite** - No estaba configurado /api → añadido
9. **PUT endpoint** - Para actualizar categorías → añadido
10. **Duplicados** - 9 registros duplicados en marzo eliminados

#### Reglas de Categorización
- Endpoint `POST /api/finanzas/aplicar-reglas-todas` creado
- Botón "Actualizar Todo" funciona
- Botón "Guardar" añadido al crear reglas

#### Gráficos
- "Todos" año → usa summary.por_categoria completo
- Gastos usan ABS() para cálculo correcto

### Artemis Tracker
- Links de tracking añadidos a Artemis.vue
- NASA AROW, ISS Info, 3D Tracker

## Decisiones Tomadas
1. **Importes siempre positivos** - El tipo "egreso"/"ingreso" indica el signo visual
2. **Categoría por defecto: Otros** - Para poder aplicar reglas
3. **Backend corrupto** - Hay que hacer backup antes de editar server.py

## Estados de Servicios
| Servicio | Status |
|----------|--------|
| Backend (8000) | ✅ |
| Frontend (3000) | ✅ |
| PostgreSQL (5432) | ✅ |

## Notas Técnicas
- server.py tiene习惯 de truncarse durante ediciones con sed
- Backend necesita `rm -rf __pycache__` antes de reiniciar
- PostgreSQL password: postgres

---

## Qdrant Vector Memory (2026-04-03)

### Instalado
- Qdrant Docker en puerto 6333
- Ollama con modelo nomic-embed-text
- 32 documentos indexados

### Script
`/root/.openclaw/workspace/scripts/vector_memory.py`

### Comandos
```bash
python3 vector_memory.py index    # Reindex todo
python3 vector_memory.py search <query>  # Buscar
python3 vector_memory.py stats    # Stats
```

### Dashboard Qdrant
http://localhost:6333/dashboard
