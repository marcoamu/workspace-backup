# 2026-03-14

## Eventos

### Claude Code + LiteLLM
- Instalado Claude Code (`npm install -g @anthropic-ai/claude-code`)
- LiteLLM no se instaló por conflictos de dependencias del sistema
- Intentando instalar LangGraph - en progreso

### MiMiniMax Agent Orchestrator
- Creado directorio: `/root/.openclaw/workspace/agents/mibimax/`
- Creado archivo: `orchestrator.py` con estructura LangGraph
- Arquitectura: Router → Research/Finance/Content → Summarize

### Fixes realizados
- Transcription-manager.html recreado completamente (error de sintaxis)
- Delete endpoint actualizado para manejar múltiples plataformas
- Add-creator ahora verifica duplicados antes de guardar
- Botón "Extraer" añadido a add-creator.html
- Fix columnas: `seguidores` → `followers`

## Pendiente
- Terminar instalación de LangGraph
- Probar orchestrator.py con MiniMax
- Conectar Claude Code via LiteLLM (pendiente)

### Finanzas Vue.js - Completado
- Frontend migrado a Vue.js en `/root/.openclaw/workspace/mibimax-app/frontend/`
- Backend FastAPI en `/root/.openclaw/workspace/mibimax-app/backend/server.py`
- Puerto 3000 (Vue), Puerto 8000 (FastAPI)
- API verificada funcionando: /api/finanzas/summary, /api/finanzas/transacciones, /api/finanzas/categorias, /api/finanzas/periodos
- 29 categorías financieras en la base de datos

### Funcionalidades implementadas
- ✅ Pestañas: Transacciones, Sin Categoría, Categorías, Reglas
- ✅ Filtros por año, tipo y búsqueda
- ✅ Categorización manual inline en tabla de transacciones
- ✅ Crear regla + aplicar a todas las que coincidan (POST /api/finanzas/aplicar-regla)
- ✅ Lista de transacciones sin categorizar (GET /api/finanzas/transacciones-sin-cat)
- ✅ CRUD completo de categorías
- ✅ Resumen: ingresos, gastos, balance

### Problemas resueltos
- Error "column 'presupuesto' does not exist" - corregido
- Múltiples reinicios del servidor por conflictos de puerto
- Errores de sintaxis en server.py - reescrito completamente
